<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Asura Scraper Data</title>
	</head>
	<body>
		<h1>Scraper Data</h1>
		<ul>
			<% data.forEach(item => { %>
			<li>
				<%= item.scanlationSite %> <br />
				<%= item.title %> <br />
				<%= item.srcImg %> <br />
				<%= item.websiteUrl %> <br />
				<%=item.chapter %>
			</li>
			<!-- Assuming each item in the data has a 'title' property -->
			<% }); %>
		</ul>
	</body>
</html>
